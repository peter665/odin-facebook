<%= form_for @new_post, url: posts_path do |f| %>
  <div class="form-group">
    <%= f.text_area :content, class: 'form-control', placeholder: 'Your post...' %>
  </div>

  <div class="text-center">
    <%= f.submit "Post It!", class: 'btn btn-primary btn-lg btn-block' %>
  </div>

<% end %>
